services:
  web:
    build: .
    ports:
      - "5000:5000"
    # Décommentez après le Fix #1 (suppression des secrets du code) :
    # environment:
    #   - API_KEY=demo_key_pour_atelier
    #   - DB_PASSWORD=demo_pwd_pour_atelier
